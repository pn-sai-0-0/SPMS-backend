check whether the port is free and the database connectivity have right database and credentials.
to run use : ".\mvnw.cmd spring-boot:run"
it the above one showed error use ".\mvnw.cmd clean spring-boot:run" and still error popped up check the connectivity recheck the point 1 is valid on your system and you had installed all the requirenments
http://localhost:<port_used>,  application will run on this